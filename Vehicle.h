#ifndef VEHICLE_H
#define VEHICLE_H

#include <string>

class Vehicle{

    int id;
    std::string model;
    std::string brand;
    std::string FirstStop;
    std::string LastStop;
    int run;

};

#endif